import { Meta } from "@storybook/addon-docs/blocks"

import {
	RadiusScale,
	ShadowScale,
} from "@workspace/ui/foundations/radius-shadow-scale"

<Meta title="Foundations/Radius & Shadows" />

# Radius & Shadows

## Radius

One source value drives the whole scale:

```css
--radius: 0.625rem;
```

The `@theme inline` block derives every step, `xs` through `4xl`, from it with multipliers (`0.2` to `2.6`), so changing `--radius` reshapes every component at once.

<RadiusScale />

## Shadows

**`globals.css` declares no shadow tokens.** The system uses the Tailwind v4 default elevation scale as-is. Introducing custom shadow tokens is a design decision, not a story author's call.

<ShadowScale />
